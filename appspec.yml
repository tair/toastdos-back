version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/codedeploy-back-landingpad/
hooks:
  AfterInstall:
    - location: server-scripts/after-install
      timeout: 300
      runas: root

